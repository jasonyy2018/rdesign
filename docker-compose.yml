services:
  ai-zhishengji:
    container_name: ai-zhishengji-web
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - '8084:8080'
    environment:
      - BACKEND_URL=http://119.91.202.144:3399
      - ARTICLES_URL=http://119.91.202.144:3210
      - PORT=8080
    # 直接通过 IP:端口 访问，无需 Nginx
    # 访问方式: http://<服务器IP>:8084

    ## 执行命令
    ## docker-compose down
    ## docker-compose up --build -d
