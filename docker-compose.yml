services:
  ai-zhishengji:
    container_name: ai-zhishengji-web
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - '8084:8080'
    environment:
      - BACKEND_URL=http://119.91.202.144:3399
      - ARTICLES_URL=http://119.91.202.144:3210
      - PORT=8080
    # 移除了 Nginx 相关的卷挂载，因为现在是直接 Node.js 部署
    # 项目网址: https://rdes.togomol.com/

    ## 执行命令
    ## docker-compose down
    ## docker-compose up --build -d
